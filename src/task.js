class Task {
  constructor(id, title, notes, dueDate, priority, list) {
    this.id = id;
    this.title = title;
    this.notes = notes;
    this.dueDate = dueDate;
    this.priority = priority;
    this.list = list;
  }
}

export default Task;
